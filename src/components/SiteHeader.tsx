export default function SiteHeader() {
  return (
    <header className="site-header">
      <a className="wordmark" href="#hero">Strata</a>
      <a className="header-link" href="#reserve">Reserve</a>
    </header>
  );
}
